# Orchestrator Agent Workflow

Orchestrator Agent의 전체 워크플로우를 상세히 설명합니다.

에이전트 정의: [`.claude/agents/orchestrator-agent.md`](../../.claude/agents/orchestrator-agent.md)
에이전트 협업 가이드: [`agent-collaboration.md`](agent-collaboration.md)

---

## 워크플로우 개요

Orchestrator Agent는 **PM(Project Manager)** 역할로, 직접 코드를 작성하지 않고 **접수 → 계획 → 실행 → 완료** 4단계 프로세스로 작업을 조율합니다.

```
사용자 요청
    │
    ▼
┌──────────────────┐
│ 🎯 Orchestrator  │
│    작업 접수      │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐     ┌───────────────────────────────────┐
│ /plan-work       │────▶│ 계획서: WBS + 의존성 + 병렬 그룹  │
│ (MANDATORY)      │     │ + 실행 구성안 + 에이전트 배정      │
└──────────────────┘     └──────────────┬────────────────────┘
                                        │
                              사용자 승인 ✅
                                        │
                    ┌───────────────────┼───────────────────┐
                    │                   │                   │
                    ▼                   ▼                   ▼
           ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
           │ Teams 구성    │   │ 순차 실행     │   │ 직접 실행     │
           │ + Worktree   │   │ 서브에이전트  │   │ /work        │
           └───────┬──────┘   └──────┬───────┘   └──────┬───────┘
                   │                 │                   │
      ┌────────────┼────────┐       │                   │
      │            │        │       │                   │
      ▼            ▼        ▼       ▼                   ▼
  ┌────────┐ ┌────────┐ ┌────────┐
  │Agent A │ │Agent B │ │Agent C │  (병렬/순차 실행)
  │worktree│ │worktree│ │worktree│
  └───┬────┘ └───┬────┘ └───┬────┘
      │          │          │
      └──────────┼──────────┘
                 │
                 ▼
        ┌────────────────┐
        │ 결과 통합       │
        │ PR → 리뷰 → 머지│
        └───────┬────────┘
                │
                ▼
        ┌────────────────┐
        │ 📊 완료 보고    │
        └────────────────┘
```

---

## Phase 0: 작업 접수

사용자의 요청을 접수하고, 요구사항의 유형을 분류합니다.

```
┌─────────────────────────────────────────────────────────────────┐
│                      사용자 요청 접수                            │
├─────────────┬─────────────────┬──────────────────┬──────────────┤
│ 버그 리포트  │ 기능 요청        │ 마일스톤 작업     │ 대화 컨텍스트 │
└──────┬──────┴────────┬────────┴────────┬─────────┴──────┬───────┘
       │               │                 │                │
       └───────────────┴─────────────────┴────────────────┘
                                │
                                ▼
                   ┌────────────────────────┐
                   │  Orchestrator Agent    │
                   │  (🎯 PM 역할 접수)      │
                   └────────────┬───────────┘
                                │
                                ▼
                      ❌ 직접 코드 수정 금지
                      ✅ /plan-work 실행
```

**핵심 원칙**: Orchestrator는 절대 직접 코드를 수정하지 않습니다. 반드시 `/plan-work`로 계획을 수립한 후 전문 에이전트에게 위임합니다.

---

## Phase 1: 업무 계획 수립 (`/plan-work` — MANDATORY)

모든 작업은 반드시 `/plan-work` 스킬로 계획을 먼저 수립합니다.

```
❌ WRONG: 이슈 접수 → 바로 에이전트 배정 → 코드 작성
✅ RIGHT: 이슈 접수 → /plan-work 계획 수립 → 계획 승인 → /work 실행
```

### `/plan-work` 실행 옵션

```bash
/plan-work                        # 대화 컨텍스트 기반 계획 수립
/plan-work --issue <N>            # 단일 이슈 작업 계획
/plan-work --milestone <N>        # 마일스톤 전체 작업 계획
/plan-work --issues <N1,N2,...>   # 복수 이슈 작업 계획
```

### 계획 수립 6단계 프로세스

```
┌──────────────────────────────────────────────────────────────────────┐
│                     /plan-work 실행 (필수)                           │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ① 정보 수집                                                        │
│  ┌──────────────┬──────────────────┬────────────────────┐           │
│  │ --issue <N>  │ --milestone <N>  │ --issues <N1,N2>   │           │
│  │ gh issue view│ gh api milestones│ 각 이슈 정보 수집   │           │
│  └──────┬───────┴────────┬─────────┴──────────┬─────────┘           │
│         └────────────────┴────────────────────┘                      │
│                          │                                           │
│  ② 작업 분해 (WBS)       ▼                                          │
│  ┌──────────────────────────────────────────────────┐               │
│  │ Epic (마일스톤)                                    │               │
│  │  └─ Story (GitHub Issue)                          │               │
│  │      └─ Task (개별 작업 항목)                      │               │
│  │          └─ Sub-task (세부 구현 단계)               │               │
│  └──────────────────────┬───────────────────────────┘               │
│                          │                                           │
│  ③ 모듈 영향 분석        ▼                                          │
│  ┌──────────┬────────┬──────────┬──────────────┬──────────┐        │
│  │ shared/  │ cli/   │ api/     │ console/     │ platform/│        │
│  │core-agent│cli-agnt│backend-ag│frontend-agent│devops-ag │        │
│  └────┬─────┴───┬────┴────┬─────┴──────┬───────┴────┬─────┘        │
│       │         │         │            │            │               │
│  ④ 의존성 분석  ▼         ▼            ▼            ▼               │
│  ┌──────────────────────────────────────────────────┐               │
│  │ Task A (shared) ─┬→ Task B (api)                 │               │
│  │                  └→ Task C (console)              │               │
│  │ Task B ──→ Task D (e2e)                          │               │
│  │ Task C ──→ Task D                                │               │
│  └──────────────────────┬───────────────────────────┘               │
│                          │                                           │
│  ⑤ 병렬 그룹 분류        ▼                                          │
│  ┌──────────────────────────────────────────────────┐               │
│  │ P1: [Task A]              ← 선행 조건 없음       │               │
│  │ P2: [Task B, Task C]      ← P1 완료 후 (병렬)    │               │
│  │ P3: [Task D]              ← P2 완료 후           │               │
│  └──────────────────────┬───────────────────────────┘               │
│                          │                                           │
│  ⑥ 실행 구성안 + 설계 판단 ▼                                        │
│  ┌──────────────────────────────────────────────────┐               │
│  │ Option A: Claude Code Teams (병렬 2+ 작업)        │               │
│  │ Option B: 순차 서브에이전트 (순차 의존성만)         │               │
│  │ Option C: 직접 실행 (단일 파일 간단 수정)          │               │
│  │                                                   │               │
│  │ 설계 필요? → system/backend/frontend architect    │               │
│  │ UI 필요?  → frontend-design skill                 │               │
│  └──────────────────────────────────────────────────┘               │
│                                                                      │
├──────────────────────────────────────────────────────────────────────┤
│                    📋 실행 계획서 출력                                │
└──────────────────────────────────────┬───────────────────────────────┘
                                       │
                                       ▼
                            ┌────────────────────┐
                            │  사용자 승인 요청   │
                            └────────┬───────────┘
                            ┌────────┴───────────┐
                            │                    │
                         승인 ✅              거부 ❌
                            │                    │
                            ▼                    ▼
                       Phase 2로          계획 수정 후 재제출
```

### 계획서 산출물

| 항목 | 설명 |
|------|------|
| **작업 분해 (WBS)** | Epic → Story → Task → Sub-task 단위 분해 |
| **의존성 그래프** | 모듈/파일/인터페이스 의존성 도출 |
| **병렬 실행 그룹** | 동시 실행 가능한 작업 그룹 식별 |
| **실행 구성안** | Claude Code Teams vs 순차 서브에이전트 제안 |
| **에이전트 배정** | 각 태스크에 적합한 전문 에이전트 지정 |
| **설계 필요 판단** | Architect/Design 에이전트 활용 여부 식별 |

---

## Phase 2: 실행 구성 판단

계획서가 승인되면, 작업 성격에 따라 3가지 실행 방식 중 하나를 선택합니다.

```
                         계획 승인됨
                             │
              ┌──────────────┼──────────────┐
              │              │              │
              ▼              ▼              ▼
    ┌─────────────┐ ┌──────────────┐ ┌──────────────┐
    │ 병렬 작업    │ │ 순차 작업만  │ │ 단일 파일    │
    │ 2개 이상     │ │ 존재         │ │ 간단 수정    │
    │ + 다른 모듈  │ │              │ │              │
    └──────┬──────┘ └──────┬───────┘ └──────┬───────┘
           │               │                │
           ▼               ▼                ▼
    ┌─────────────┐ ┌──────────────┐ ┌──────────────┐
    │ Claude Code │ │ 순차          │ │ /work        │
    │ Teams       │ │ 서브에이전트  │ │ --issue <N>  │
    │ + Worktree  │ │ 순차 실행     │ │ 직접 실행    │
    └──────┬──────┘ └──────┬───────┘ └──────┬───────┘
           │               │                │
           ▼               ▼                ▼
       Phase 3A        Phase 3B         Phase 3C
```

### 실행 방식 판단 기준

| 조건 | 실행 방식 |
|------|-----------|
| 병렬 작업 2개 이상 + 서로 다른 모듈 | **Claude Code Teams** (팀 + Git Worktree) |
| 병렬 작업 2개 이상 + 동일 모듈 다른 파일 | **Claude Code Teams** + Git Worktree |
| 순차 의존성만 존재 | **순차 서브에이전트** |
| 단일 파일 수정의 간단한 작업 | **직접 실행** (`/work --issue <N>`) |

---

## Phase 3A: Claude Code Teams 실행 (병렬 작업)

병렬 작업이 가능한 경우, Claude Code Teams로 팀을 구성하여 동시 실행합니다.

```
┌──────────────────────────────────────────────────────────────────┐
│                    Claude Code Teams 실행                        │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Step 1: TeamCreate("feature-432-backup")                       │
│                          │                                       │
│  Step 2: TaskCreate      ▼                                       │
│  ┌───────────┬───────────────┬───────────────┬──────────────┐   │
│  │ Task 1    │ Task 2        │ Task 3        │ Task 4       │   │
│  │ 분석      │ API 구현       │ UI 구현       │ E2E 테스트   │   │
│  │ (pending) │ (pending)     │ (pending)     │ (pending)    │   │
│  └─────┬─────┴───────┬───────┴───────┬───────┴──────┬───────┘   │
│        │             │               │              │           │
│  Step 3: 의존성 설정  │               │              │           │
│        │     blockedBy:[1]    blockedBy:[1]   blockedBy:[2,3]   │
│        │             │               │              │           │
│  Step 4: 설계 필요 여부 판단                                     │
│  ┌──────────────────────────────────────────────────────┐       │
│  │ 새 API 설계 필요? → backend-architect 에이전트 위임   │       │
│  │ 새 UI 설계 필요? → frontend-design skill 실행         │       │
│  │ 설계 불필요?     → 바로 구현 에이전트 배정             │       │
│  └──────────────────────────────────────────────────────┘       │
│        │                                                        │
│  Step 5: P1 실행 (독립 작업)                                    │
│        ▼                                                        │
│  ┌─────────────┐                                                │
│  │ core-agent  │  ← isolation: "worktree"                       │
│  │ Task 1 실행 │                                                │
│  └──────┬──────┘                                                │
│         │ 완료                                                   │
│         ▼                                                        │
│  Step 6: P2 실행 (병렬)                                         │
│  ┌─────────────┐     ┌────────────────┐                         │
│  │ backend-agt │     │ frontend-agent │  ← 각각 worktree        │
│  │ Task 2 실행 │     │ Task 3 실행    │                         │
│  └──────┬──────┘     └───────┬────────┘                         │
│         │                    │                                   │
│         │  ◄── SendMessage ──┤  (SYNC: API 스펙 공유)           │
│         │                    │                                   │
│         └────────┬───────────┘                                   │
│                  │ 모두 완료                                     │
│                  ▼                                                │
│  Step 7: P3 실행 (통합)                                         │
│  ┌──────────────┐                                               │
│  │ devops-agent │  ← isolation: "worktree"                      │
│  │ Task 4 실행  │                                               │
│  └──────┬───────┘                                               │
│         │ 완료                                                   │
│         ▼                                                        │
│  Step 8: 결과 통합                                               │
│  ┌──────────────────────────────────────────┐                   │
│  │ 각 worktree 커밋 → 메인 저장소 merge     │                   │
│  │ PR 생성 → 코드 리뷰 → 머지               │                   │
│  └──────────────────────────────────────────┘                   │
│         │                                                        │
│  Step 9: TeamDelete("feature-432-backup")                       │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

### Teams 구성 도구

| 단계 | 도구 | 설명 |
|------|------|------|
| 팀 생성 | `TeamCreate` | 팀과 태스크 리스트 생성 |
| 태스크 생성 | `TaskCreate` | 개별 작업 항목 등록 |
| 의존성 설정 | `TaskUpdate` | `addBlocks`/`addBlockedBy`로 순서 관리 |
| 에이전트 배치 | `Agent` | `team_name` + `isolation: "worktree"` |
| 동기화 | `SendMessage` | 에이전트 간 결과 공유 |
| 정리 | `TeamDelete` | 팀 리소스 정리 |

---

## Phase 3B: 순차 서브에이전트 실행

순차 의존성만 있는 경우, 서브에이전트를 순서대로 실행합니다.

```
┌──────────────────────────────────────────────┐
│           순차 서브에이전트 실행               │
├──────────────────────────────────────────────┤
│                                              │
│  ① core-agent ──→ 인터페이스 정의            │
│         │ 완료                               │
│         ▼                                    │
│  ② backend-agent ──→ API 구현                │
│         │ 완료                               │
│         ▼                                    │
│  ③ frontend-agent ──→ UI 구현                │
│         │ 완료                               │
│         ▼                                    │
│  ④ devops-agent ──→ E2E 테스트               │
│         │ 완료                               │
│         ▼                                    │
│  ⑤ PR 생성 → 코드 리뷰 → 머지               │
│                                              │
└──────────────────────────────────────────────┘
```

---

## Phase 3C: 직접 실행

단일 파일 수정 등 간단한 작업은 `/work` 명령으로 직접 실행합니다.

```
┌──────────────────────────────────────────────┐
│              직접 실행                        │
├──────────────────────────────────────────────┤
│                                              │
│  /work --issue <N>                           │
│         │                                    │
│         ▼                                    │
│  ┌─────────────────────────────────┐        │
│  │ 브랜치 생성                      │        │
│  │ → TDD (Red → Green → Refactor)  │        │
│  │ → 커밋                           │        │
│  │ → PR 생성                        │        │
│  │ → 코드 리뷰                      │        │
│  │ → 머지                           │        │
│  └─────────────────────────────────┘        │
│                                              │
└──────────────────────────────────────────────┘
```

---

## Phase 4: 완료 및 보고

모든 작업이 완료되면 체크리스트를 확인하고 결과를 보고합니다.

```
┌──────────────────────────────────────────────────────────────┐
│                      완료 체크리스트                          │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ✅ 확인 항목                                                │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ □ 모든 이슈 완료 (GitHub Issue 체크박스 100%)        │    │
│  │ □ 모든 동기화 포인트 실행됨                          │    │
│  │ □ 단위 테스트 통과                                   │    │
│  │ □ E2E 테스트 통과                                    │    │
│  │ □ 코드 리뷰 완료                                     │    │
│  │ □ PR 머지 완료                                       │    │
│  │ □ 문서 업데이트 (필요 시)                             │    │
│  └─────────────────────────────────────────────────────┘    │
│                          │                                   │
│                          ▼                                   │
│  📊 진행 보고서 작성                                        │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ Overall: X/Y (100%)                                 │    │
│  │ By Agent: Core ✅ | CLI ✅ | Backend ✅ | ...        │    │
│  │ Merged PRs: #435, #436, ...                         │    │
│  └─────────────────────────────────────────────────────┘    │
│                          │                                   │
│                          ▼                                   │
│               사용자에게 결과 보고                            │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 설계 에이전트 호출 분기

`/plan-work` 단계에서 설계가 필요한 작업이 식별되면, 구현 전에 Architect 에이전트를 먼저 호출합니다.

```
                  설계 필요 판단
                       │
        ┌──────────────┼──────────────────┬────────────────┐
        │              │                  │                │
        ▼              ▼                  ▼                ▼
  ┌───────────┐  ┌───────────┐   ┌────────────┐   ┌────────────┐
  │ system-   │  │ backend-  │   │ frontend-  │   │ frontend-  │
  │ architect │  │ architect │   │ architect  │   │ design     │
  │           │  │           │   │            │   │ (skill)    │
  │ 전체 구조 │  │ API/DB    │   │ 컴포넌트   │   │ UI/UX      │
  │ 모듈 간   │  │ 스키마    │   │ 상태 관리  │   │ 시각 설계  │
  │ 인터페이스│  │ 인증/인가 │   │ 라우팅     │   │ 디자인     │
  └─────┬─────┘  └─────┬─────┘   └─────┬──────┘   └─────┬──────┘
        │              │               │                │
        │   설계 산출물 전달             │                │
        ▼              ▼               ▼                ▼
  ┌───────────┐  ┌───────────┐   ┌────────────┐   ┌────────────┐
  │ core-agent│  │backend-agt│   │frontend-agt│   │frontend-agt│
  │ 구현      │  │ 구현      │   │ 구현       │   │ 구현       │
  └───────────┘  └───────────┘   └────────────┘   └────────────┘
```

### 설계 에이전트 활용 판단 기준

| 상황 | 활용할 에이전트 | 산출물 |
|------|----------------|--------|
| 새로운 기능의 전체 구조 설계 | `system-architect` | 아키텍처 설계서 |
| REST API 엔드포인트/DB 스키마 | `backend-architect` | API/DB 설계서 |
| 새로운 페이지/컴포넌트 구조 | `frontend-architect` | 컴포넌트 설계서 |
| UI/UX 디자인, 화면 레이아웃 | `frontend-design` skill | UI 디자인 |
| 모듈 간 인터페이스 정의 | `system-architect` | 인터페이스 명세 |
| Docker/배포 아키텍처 | `devops-architect` | 배포 설계서 |
| 단순 버그 수정, 기존 패턴 | 설계 불필요 | - |

---

## 동기화 포인트 (Sync Points)

에이전트 간 핸드오프가 필요한 시점입니다. `SendMessage`를 통해 결과를 공유합니다.

```
┌──────────────┐                          ┌──────────────┐
│  core-agent  │ ──Interface Ready──────▶ │  All agents  │
└──────────────┘                          └──────────────┘

┌──────────────┐                          ┌──────────────┐
│ backend-agt  │ ──API Spec Ready───────▶ │frontend-agent│
└──────────────┘                          └──────────────┘

┌──────────────┐                          ┌──────────────┐
│ backend-agt  │ ──OpenAPI Ready────────▶ │frontend-agent│
└──────────────┘                          └──────────────┘

┌──────────────────────┐                  ┌──────────────┐
│ backend + frontend   │ ──Images Ready─▶ │ devops-agent │
└──────────────────────┘                  └──────────────┘

┌──────────────┐                          ┌──────────────┐
│ devops-agent │ ──Env Ready────────────▶ │  All agents  │
└──────────────┘                          └──────────────┘
```

| Sync Point | From | To | 공유 내용 |
|------------|------|----|-----------|
| Interface Ready | Core | All | 공유 인터페이스/타입 사용 가능 |
| API Spec Ready | Backend | Frontend | BFF 프록시 구현을 위한 API 스펙 |
| OpenAPI Ready | Backend | Frontend | API 클라이언트 자동 생성용 |
| Images Ready | Backend+Frontend | DevOps | Docker 이미지 빌드 가능 |
| Env Ready | DevOps | All | 통합 테스트 환경 준비 완료 |

---

## 에러 복구

### Blocker 처리 흐름

```
에이전트가 블로커 보고
         │
         ▼
┌────────────────────────┐
│ Orchestrator 분석       │
├────────────────────────┤
│ Option A: 대기          │ ← 의존 작업이 곧 완료 예정
│ Option B: 부분 스펙 공유│ ← 선행 에이전트에 조기 공유 요청
│ Option C: 작업 순서 변경│ ← 다른 태스크 먼저 진행
└────────────────────────┘
```

### 통합 테스트 실패

```
E2E 테스트 실패
    │
    ▼
실패 로그 분석 → 원인 컴포넌트 식별
    │
    ▼
해당 에이전트에 수정 요청 → 수정 후 재테스트
```

---

## 요약 테이블

| Phase | 단계 | 수행자 | 산출물 |
|-------|------|--------|--------|
| **0** | 작업 접수 | Orchestrator | 요구사항 정리 |
| **1** | 업무 계획 수립 | `/plan-work` (필수) | 실행 계획서 (WBS, 의존성, 병렬 그룹) |
| **1.5** | 설계 (필요시) | Architect 에이전트 | 아키텍처/API/UI 설계서 |
| **2** | 실행 구성 판단 | Orchestrator | Teams / 순차 / 직접 실행 결정 |
| **3** | 작업 실행 | 전문 에이전트들 | 코드, 테스트, 문서 |
| **4** | 통합 및 완료 | Orchestrator | PR 머지, 진행 보고서 |

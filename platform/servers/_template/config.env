# =============================================================================
# Server Configuration Template
# =============================================================================
# IMPORTANT: Use create-server.sh script to create a new server:
#   ./scripts/create-server.sh <your-server-name> [PAPER|VANILLA|FORGE|FABRIC]
#
# The server name you provide will be used for:
#   - Directory name: servers/<server-name>/
#   - Service name: mc-<server-name>
#   - Container name: mc-<server-name>
#   - Hostname: <server-name>.local
#
# After creation, edit this file to customize your server settings.
# =============================================================================

# -----------------------------------------------------------------------------
# Server Type
# -----------------------------------------------------------------------------
TYPE=PAPER
# Options: VANILLA, PAPER, SPIGOT, FORGE, FABRIC, NEOFORGE, QUILT, MODRINTH, AUTO_CURSEFORGE
# See: https://docker-minecraft-server.readthedocs.io/en/latest/types-and-platforms/

# Minecraft Version
VERSION=1.20.4

# -----------------------------------------------------------------------------
# Resources
# -----------------------------------------------------------------------------
MEMORY=4G
# Recommended:
#   VANILLA/PAPER: 2G
#   FORGE/FABRIC: 4G
#   Modpacks: 6G+

# -----------------------------------------------------------------------------
# Game Settings
# -----------------------------------------------------------------------------
GAMEMODE=survival
# Options: survival, creative, adventure, spectator

DIFFICULTY=normal
# Options: peaceful, easy, normal, hard

MAX_PLAYERS=20
VIEW_DISTANCE=10
PVP=true
SPAWN_PROTECTION=0

# -----------------------------------------------------------------------------
# World
# -----------------------------------------------------------------------------
# World directory: /worlds/ (shared across servers)
# LEVEL specifies the world folder name within /worlds/
LEVEL=world

# Store world in shared /worlds/ directory (not in server's /data/)
EXTRA_ARGS=--universe /worlds/

# Option 1: Default - new world with random seed (default)
# Option 2: Specific seed (set via create-server.sh --seed)
# SEED=12345
# Option 3: Download from URL (set via create-server.sh --world-url)
# WORLD=https://example.com/world.zip

# -----------------------------------------------------------------------------
# RCON (Password inherited from .env)
# -----------------------------------------------------------------------------
ENABLE_RCON=true

# -----------------------------------------------------------------------------
# Server MOTD (Message of the Day)
# -----------------------------------------------------------------------------
MOTD=Welcome! Your adventure begins here.

# -----------------------------------------------------------------------------
# Optional: Mods/Plugins
# -----------------------------------------------------------------------------
# MODRINTH_PROJECTS: |
#   fabric-api
#   lithium

# SPIGET_RESOURCES: "1234,5678"

# ⚠️ IMPORTANT: Auto-download mod dependencies from Modrinth
# Options: required, optional, none
# - required: Download required dependencies automatically (RECOMMENDED)
# - optional: Download required + optional dependencies
# - none: Don't download dependencies (manual management)
MODRINTH_DOWNLOAD_DEPENDENCIES=required

# -----------------------------------------------------------------------------
# Optional: Modpack Configuration (MODRINTH / AUTO_CURSEFORGE types only)
# -----------------------------------------------------------------------------
# For MODRINTH type:
# MODRINTH_MODPACK=fabric-example
# MODRINTH_VERSION=1.0.0 (optional - defaults to latest)
# MODRINTH_LOADER=fabric (optional - auto-detected from modpack)

# For AUTO_CURSEFORGE type:
# CF_SLUG=forge-example
# CF_VERSION=2.0.0 (optional - defaults to latest)
# CF_LOADER=forge (optional - auto-detected from modpack)

# -----------------------------------------------------------------------------
# Authentication
# -----------------------------------------------------------------------------
ONLINE_MODE=TRUE
# Set to FALSE to allow cracked clients (not recommended)

# -----------------------------------------------------------------------------
# Whitelist (Secure by Default)
# -----------------------------------------------------------------------------
ENABLE_WHITELIST=TRUE
# WHITELIST=player1,player2
# OPS=admin1,admin2

# -----------------------------------------------------------------------------
# Optional: Advanced
# -----------------------------------------------------------------------------
# USE_AIKAR_FLAGS=true
# ICON=https://example.com/icon.png
